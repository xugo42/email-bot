# -*- coding: utf-8 -*-
"""
邮件自动取件机器人 —— 配置文件
把下面的占位符改成你自己的信息，保存即可。改完跑一遍 测试配置.bat 确认。
"""

# ============ 邮箱设置（必填）============
EMAIL_ADDR = "你的邮箱@example.com"      # 机器人收信的邮箱（如 xxx@126.com）
EMAIL_AUTH_CODE = "你的16位授权码"        # 在邮箱网页版开启 IMAP/SMTP 后生成的授权码，不是登录密码！

# IMAP/SMTP 服务器，按你的邮箱服务商填：
#   QQ 邮箱:  imap.qq.com  /  smtp.qq.com
#   163/126: imap.163.com / smtp.163.com
IMAP_SERVER = "imap.126.com"
IMAP_PORT = 993
SMTP_SERVER = "smtp.126.com"
SMTP_PORT = 465

# ============ 安全设置（建议保留）============
# 只响应来自这些发件人的指令，多个用逗号分隔。留空=任何人都能指挥机器人（不安全）
ALLOWED_SENDERS = "你的手机邮箱@example.com"

# ============ 搜索设置 ============
# 慢速搜索的目录（没装 Everything 时用）。留空则自动扫描所有磁盘（较慢）
SEARCH_DIRS = ["C:/Users/<你的用户名>"]

# Everything 的 es.exe 路径（可选，装了 Everything 才有全盘秒搜）
# 留空则自动探测标准安装路径，找不到就退回慢速遍历
ES_EXE = ""

# ============ AI 问答设置（可选）============
# 邮件发「问 问题」时调用的 AI。填你的 DeepSeek API Key；留空则「问」功能不可用
DEEPSEEK_API_KEY = ""
DEEPSEEK_BASE_URL = "https://api.deepseek.com"
DEEPSEEK_MODEL = "deepseek-v4-flash"

# ============ AI 记忆设置 ============
# 对话记忆：记住同一发件人最近几轮问答（0 = 关闭记忆）
CHAT_HISTORY_TURNS = 6
# 单个发件人的记忆总字数上限，超出自动修剪最旧对话（记忆会存到文件，重启后仍保留）
AI_MEMORY_MAX_CHARS = 8000
# AI 回答风格设定（可随意改）
AI_SYSTEM_PROMPT = """你是「邮件机器人」，一个通过邮件远程控制电脑的 AI 助手，能帮你找文件、发文件、答疑。同时你是一位高效、直接的研究型助手。

## 回答风格
- 直接给出结论和可验证的信息，跳过寒暄、共情、免责声明。
- 能列点就列点，能用表格就用表格，少用叙事段落。
- 结尾不要"希望这对你有帮助"之类的套话。
- 不替用户做决定；需求模糊时先指出并请用户澄清，不要自行脑补。

## 准确性
- 只回答你有把握的内容；不确定的信息标注「未确认」。
- 知识有截止日期。涉及时效性信息时，先声明"我的知识可能过时"。
- 不知道就说不知道，绝不编造。

## 边界
- 在合法合规范围内直接回答；遇到确实不能做的事，简短说明原因即可，不啰嗦、不说教。"""

# ============ 托盘图标 ============
# 自定义托盘图标（.ico 或 .png，64x64 最佳）。留空用默认信封图标
TRAY_ICON_PATH = ""

# ============ 其他 ============
# 一次最多回传多少个文件附件
MAX_ATTACHMENTS = 3
# 单个附件最大字节（默认 48MB，邮箱附件普遍限制 50MB 以内）
MAX_ATTACH_SIZE = 48 * 1024 * 1024
# 每轮检查新邮件的时间间隔（秒）
POLL_INTERVAL = 60
# 日志文件最大字节数，超过自动清空（默认 500KB）
LOG_MAX_SIZE = 500 * 1024
